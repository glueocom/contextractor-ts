# Spelling Autofix Report

**Date**: 2026-05-12
**Tool**: cspell (npx)
**Scope**: `apps/**`, `packages/**`, `tools/**`, `.claude/**`, root `*.md`, `SPEC.md`, `CLAUDE.md`, `AGENTS.md`
**Excludes**: `node_modules/`, `dist/`, `target/`, `examples/`, `autonomous-task-output/`, `apps/apify-actor/storage/`, `apps/standalone/dist/`

## Summary

- **Files checked**: 229
- **Issues flagged by cspell**: 195 across 50 files
- **Genuine typos fixed**: 0
- **False positives**: all 195 flags

## Genuine Typos Fixed

None. All flagged words were confirmed as false positives after review.

## False Positives

All flagged words fall into the following categories:

### Auto-generated content (do not edit)
- `DOMCONTENTLOADE` in `apps/apify-actor/README.md:64` — inside a `<!-- @generated:start name="apify-input-schema" -->` region auto-generated by `@contextractor/gen-md-regions`; truncated by line length, not a typo

### Intentional identifiers
- `ensureuninstalled` in `dev-utils/installation/lib/pkg.ts` — CLI sub-command name used consistently in comment, function dispatch, and error message
- `RAQSG` in `tools/platform-test-runner/src/apify-client.ts` — substring of Apify Actor ID `nXCKPalCKnRAQSG5S`
- `Mcpjson` — cspell splitting `enabledMcpjsonServers` (a `settings.json` field name) at a camelCase boundary; not a word error
- `qnfa` — substring of Apify run ID `QNaTc2onh9ng8qnfa` in `docs/troubleshooting/timeout/report.md`
- `miroslavsekera` — Unix username appearing in file paths throughout `.claude/` refs
- `vdrmota`, `maxcopell` — GitHub usernames in `apify-ultimate-scraper` skill
- `mnda` — intentional abbreviation in skill-creator

### Rust ecosystem terms
- `thiserror`, `reqwest`, `nextest`, `mockall`, `proptest`, `insta` — Rust crate names
- `println`, `eprintln` — Rust macros shown in code examples
- `dhat`, `callgrind`, `flamegraph`, `ahash` — Rust profiling/perf crates
- `Zscript` — `cargo -Zscript` unstable feature flag
- `rlib` — Rust library output type
- `Newtype` — established Rust design pattern name
- `doctest` — Rust `#[doc = "..."]` test term
- `codegen` — standard compiler/build abbreviation
- `roundtrips` — technical term for encode→decode cycles

### Git terminology
- `worktree`, `worktrees`, `WORKTREE` — `git worktree` is a standard git sub-command; all uses in `add-worktree.md` and `squash-merge-to-dev.md` are correct

### Markdown/prompt tooling terms
- `frontmatter`, `Frontmatter` — YAML front-matter is standard terminology across Jekyll, Hugo, Gatsby, Claude Code prompt specs; 29 occurrences across agents, commands, skills

### British spelling
- `behaviour` in `packages/crawler/SPEC.md:30` — valid British English; the section heading "Browser behaviour" is intentional

### Proper nouns
- `Technica` — part of "Ars Technica" (tech news outlet) in `docs/troubleshooting/timeout/report.md`
- `Adrien Barbaresi` — creator of the trafilatura Python library, cited in prompts

### Web/internet terms
- `webscraping` — compound form used in context of web scraping domain
- `subreddits` — Reddit-specific plural noun in `web-research-specialist` agent
- `userland` — OS/systems term for user space

### Other tools and libraries
- `pdfplumber`, `OOXML` — references in `skill-creator` to document processing libraries
- `Microbenchmarks` — standard benchmarking terminology
- `roundtrips` — encode/decode cycle terminology

## Ambiguous Items (no action taken)

None requiring a decision prompt.
