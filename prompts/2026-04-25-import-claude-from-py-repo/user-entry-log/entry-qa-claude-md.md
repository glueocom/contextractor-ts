**Q:** Should the prompt update the target `CLAUDE.md` to reference the new `.claude/rules/` directory (mirroring how the source `CLAUDE.md` has a `## Rules` section pointing at `.claude/rules/`)?

**A:** Yes — add a `## Rules` section.

After importing the rules, append a `## Rules` section to target `CLAUDE.md` listing each imported rule with a one-line summary and a link to the file. Match the source convention so rules are discoverable.
