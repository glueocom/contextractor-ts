# Q: CLAUDE.md Line Target

**Q:** Removing Do/Don't only saves ~40 lines (170→130). Compress other sections too, or relax target?

**A:** Compress all to hit <100. Also compress Commands (remove pip lines), Testing (remove code example), and Project Structure.
