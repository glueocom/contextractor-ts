# Tests Master — Stub Autonomous Maintenance

**TLDR**: Execute `run-all.sh` under `STUB_MODE=1` and assert correct output. Autofix any failures.

## Agents and Skills

- `test-runner` — run and verify the stub pipeline

## Steps

- `step-test-patch.md` — run the stub pipeline and assert expected output
- `step-test-user-intent.md` — regression check confirming no real processes launched
