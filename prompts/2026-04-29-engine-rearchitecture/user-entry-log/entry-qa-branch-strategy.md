**Q**: Should the implementation be structured as a single feature branch, or as multiple separate PRs (one per major step)?

**A**: Single branch / PR.

One feature branch for the entire rearchitecture. Implementation steps act as sequential commit checkpoints on the same branch.
