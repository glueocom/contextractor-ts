{
    "name": "contextractor-workspace",
    "version": "0.1.0",
    "private": true,
    "description": "Phase 1: skeleton only — apps/, packages/, tools/ are filled in by subsequent migration steps.",
    "type": "module",
    "engines": {
        "node": ">=22"
    },
    "packageManager": "pnpm@10.27.0",
    "scripts": {
        "build": "pnpm -r --if-present run build",
        "test": "pnpm -r --if-present run test",
        "lint": "biome check .",
        "format": "biome format --write ."
    },
    "devDependencies": {
        "@biomejs/biome": "^1.9.4",
        "@napi-rs/cli": "^2.18.4",
        "@types/node": "^22.10.0",
        "typescript": "^5.7.0",
        "vitest": "^2.1.8"
    }
}
