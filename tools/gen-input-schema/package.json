{
  "name": "@contextractor/gen-input-schema",
  "version": "0.1.0",
  "private": true,
  "description": "Build-time generator that emits Apify INPUT_SCHEMA.json from the @contextractor/schema Zod source of truth.",
  "type": "module",
  "bin": {
    "gen-input-schema": "dist/main.js"
  },
  "scripts": {
    "build": "pnpm run fix && tsc -p tsconfig.json",
    "fix": "biome check . --fix --unsafe && biome format --write . && biome check .",
    "start": "tsx src/main.ts",
    "test": "vitest run --passWithNoTests",
    "lint": "biome check ."
  },
  "dependencies": {
    "@contextractor/schema": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/node": "^25.7.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "zod": "^4.4.3"
  }
}
